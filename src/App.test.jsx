import { describe, expect, test } from "vitest";
import { fireEvent, render, screen } from "@testing-library/react";
import App from "./App";

describe("empty tests", () => {
  test("Placeholder for actual test", () => {
    // render(<App />);
  });
});
